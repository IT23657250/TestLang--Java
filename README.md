# TestLang---Java
TestLang++ (Java) – Backend API Testing DSL

This project compiles `.test` DSL files into a JUnit 5 class (`GeneratedTests.java`) that runs HTTP tests using `java.net.http.HttpClient`.

- Build: `mvn -q clean test`
- Files will be added step by step.